> * get prometheus install package
```shell
wget https://github.com/prometheus/prometheus/releases/download/v2.26.0/prometheus-2.26.0.linux-amd64.tar.gz
```

> * start prometheus
```shell
./prometheus --config.file=prometheus.yml &
```
